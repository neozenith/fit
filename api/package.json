{
  "name": "@fit/api",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun build src/index.ts --target=node --outdir=dist --format=esm --sourcemap=linked --external=@aws-sdk/* --external=@duckdb/* && mv dist/index.js dist/index.mjs 2>/dev/null || true",
    "dev": "bun run --hot src/server.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.907.0",
    "@aws-sdk/client-ssm": "3.907.0",
    "@aws-sdk/lib-dynamodb": "3.907.0",
    "@duckdb/node-api": "1.5.5-r.3",
    "zod": "4.1.12"
  }
}
