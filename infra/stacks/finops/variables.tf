# Deliberately empty of an `environment` variable.
#
# Every other stack takes one; this one cannot, because there is nothing to
# select between. A variable here would be an invitation to deploy three copies
# of an account-scoped export (ADR-0015).
