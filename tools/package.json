{
  "name": "@fit/tools",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Operator and agent tooling: seeding, the local dev runner, and session minting.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.907.0",
    "@aws-sdk/lib-dynamodb": "3.907.0",
    "@aws-sdk/client-ssm": "3.907.0"
  }
}
